/* Consider the following code that attempts to add one to a linked list that represents a base 10 number. If the input is a list, 1 -> 3 -> 9 then the list at the end of the function should be 1 -> 4 -> 0. If an empty head is provided, the function should return a new head with the value 1.

*/
